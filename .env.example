# Optional: deterministic simulation, search and comparison work without these.
# Use a Chat Completions model with function calling and JSON output support.
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=https://api.openai.com/v1
